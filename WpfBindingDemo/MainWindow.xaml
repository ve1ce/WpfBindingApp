<Window x:Class="WpfBindingDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfBindingDemo"
        xmlns:loc="clr-namespace:CodingSeb.Localization;assembly=CodingSeb.Localization"
        xmlns:locwpf="clr-namespace:CodingSeb.Localization.WPF;assembly=CodingSeb.Localization.WPF"
        mc:Ignorable="d"
        Title="{Tr 'App.Title'}"
        Height="600"
        Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#f3f3f3"
                BorderBrush="#e0e0e0"
                BorderThickness="0,0,0,1"
                Padding="10">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">
                <TextBlock Text="{Tr 'Main.LanguageLabel'}"
                           VerticalAlignment="Center" />
                <ComboBox Width="160"
                          Margin="8,0,0,0"
                          ItemsSource="{Binding AvailableLanguages, Source={x:Static loc:Loc.Instance}}"
                          SelectedItem="{Binding CurrentLanguage, Source={x:Static loc:Loc.Instance}}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={locwpf:TrLanguageIdConverter TextId=LanguageName}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Tabs}"
                    SelectedIndex="0">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding TabName}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding TabContent}" />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
